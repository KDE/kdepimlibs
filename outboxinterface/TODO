Design:
* Use d-pointers for the attributes?  Or rely on having SomeAttrV2 in case
  something needs to be added?
* Currently apps have to call DispatcherInterface::self() for the attributes to
  get registered.  Find a way to do this automatically.

Build:
* Get our own debug area.

