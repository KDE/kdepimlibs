Design:
* Use d-pointers for the attributes?  Or rely on having SomeAttrV2 in case
  something needs to be added?

Future:
* Support volatile outbox (not stored on disk).
* Support optional default folders (e.g. no sent-mail for IMAP users).

Build:
* Get our own debug area.

